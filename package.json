{
  "name": "etherplan",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "etherplan": "./src/cli.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs test/integration/*.test.mjs",
    "check": "for file in src/*.mjs src/*/*.mjs test/*.mjs test/*/*.mjs; do node --check \"$file\" || exit; done"
  },
  "dependencies": {
    "viem": "2.56.8"
  }
}
